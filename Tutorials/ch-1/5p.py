import os #importing the os module

data = os.listdir() # putting the directory list in a variable data
print(data) # printing the data variable which contains the list of files and directories in the current workspace.

